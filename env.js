// ═══════════════════════════════════════════════════════════════
// 환경변수 파일 — Vercel 빌드 시 자동으로 값이 주입됨
// ───────────────────────────────────────────────────────────────
// 로컬 개발 시: 이 파일은 빈 값 상태 (mock 모드 동작)
// Vercel 배포 시: build 스크립트가 환경변수 → 이 파일에 주입
//
// ⚠ 이 파일은 git에 커밋되지만 실제 값은 들어있지 않음
//   Vercel 환경변수에서만 실제 값 관리
// ═══════════════════════════════════════════════════════════════
window.ENV = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
